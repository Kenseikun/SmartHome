import React from "react";
import RoomList from "../components/RoomList";

const Rooms = () => {
  return (
    <div>
      <h2>Rooms</h2>
      <RoomList />
    </div>
  );
};

export default Rooms;
